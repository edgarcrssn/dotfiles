- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.zprofile:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
