- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.zprofile:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work

- shell:
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
