- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.zprofile:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/bat:
        create: true
        path: config/bat


- create:
    - ~/Work

- shell:
    - 
        command: ./setup_homebrew.zsh
        stdout: true
        stderr: true
